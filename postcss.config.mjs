/*
  postcss.config.mjs — ARCHIVO TECNICO (no hace falta tocarlo)

  PostCSS es un ayudante que procesa los archivos de estilos (CSS)
  antes de que el sitio se arme.

  En este proyecto esta practicamente vacio a proposito: la version 4
  de Tailwind ya configura sola todo lo que se necesita, asi que aqui
  no hay que agregar nada.

  Solo existe por si algun dia un desarrollador necesitara sumar
  alguna herramienta extra de estilos. Mientras tanto, se deja tal cual.
*/
export default {}
